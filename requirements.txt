# PMS — Pension Management System
# Python backend dependencies

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
pymongo[srv]==4.7.2         # MongoDB driver; [srv] adds DNS SRV support for Atlas
python-dotenv==1.0.1        # .env file loading

# Authentication & Security
bcrypt==4.1.3               # Password hashing + OTP hashing
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4

# Data processing (FSP Engine + Guardian Agent)
numpy==1.26.4
pandas==2.2.2

# Scheduling (Guardian Agent cron job)
apscheduler==3.10.4

# Validation
pydantic==2.7.1             # FastAPI uses this for request/response models

# HTTP client (for future SMS/WhatsApp gateway calls)
httpx==0.27.0
